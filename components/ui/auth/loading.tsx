// @flow
import * as React from "react";
type Props = {};
export const Loading = (props: Props) => {
   return <div className=" flex flex-col justify-center items-center w-full h-full">loading...</div>;
};
